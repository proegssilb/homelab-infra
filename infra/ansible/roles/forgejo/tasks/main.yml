---
- name: Install Docker prerequisites
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
    state: present
    update_cache: true

- name: Add Docker GPG key
  ansible.builtin.get_url:
    url: https://download.docker.com/linux/debian/gpg
    dest: /etc/apt/keyrings/docker.asc
    mode: "0644"

- name: Add Docker apt repository
  ansible.builtin.apt_repository:
    repo: >-
      deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.asc]
      https://download.docker.com/linux/debian
      {{ ansible_distribution_release }} stable
    filename: docker
    state: present

- name: Install Docker Engine and Compose plugin
  ansible.builtin.apt:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-compose-plugin
    state: present
    update_cache: true

- name: Enable and start Docker
  ansible.builtin.systemd:
    name: docker
    enabled: true
    state: started

- name: Settle udev events before touching data disk
  ansible.builtin.command:
    cmd: udevadm settle
  changed_when: false

- name: Create ext4 filesystem on data disk
  community.general.filesystem:
    fstype: ext4
    dev: "{{ forgejo_data_disk }}"

- name: Mount forgejo data disk
  ansible.posix.mount:
    path: "{{ forgejo_data_dir }}"
    src: "{{ forgejo_data_disk }}"
    fstype: ext4
    state: mounted
    opts: defaults,noatime

- name: Create forgejo sub-directories (root-owned)
  ansible.builtin.file:
    path: "{{ forgejo_data_dir }}/{{ item }}"
    state: directory
    mode: "0750"
  loop:
    - postgres

- name: Create forgejo sub-directories (forgejo-owned)
  ansible.builtin.file:
    path: "{{ forgejo_data_dir }}/{{ item }}"
    state: directory
    owner: "1000"
    group: "1000"
    mode: "0750"
  loop:
    - data

- name: Template docker-compose.yml
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: "{{ forgejo_data_dir }}/docker-compose.yml"
    mode: "0640"
  notify: Restart forgejo

- name: Pull Forgejo images
  community.docker.docker_compose_v2_pull:
    project_src: "{{ forgejo_data_dir }}"

- name: Start Forgejo stack
  community.docker.docker_compose_v2:
    project_src: "{{ forgejo_data_dir }}"
    state: present
