[Unit]
Description=PostgreSQL database backup to TrueNAS NFS
After=network.target postgresql@{{ postgresql_version }}-main.service

[Service]
Type=oneshot
User=postgres
ExecStart=/usr/local/bin/pg-backup.sh
