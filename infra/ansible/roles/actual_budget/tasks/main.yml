---
- name: Install Docker
  ansible.builtin.import_role:
    name: docker_install

- name: Settle udev events before touching data disk
  ansible.builtin.command:
    cmd: udevadm settle
  changed_when: false

- name: Create ext4 filesystem on data disk
  community.general.filesystem:
    fstype: ext4
    dev: "{{ actual_budget_data_disk }}"

- name: Mount Actual Budget data disk
  ansible.posix.mount:
    path: "{{ actual_budget_data_dir }}"
    src: "{{ actual_budget_data_disk }}"
    fstype: ext4
    state: mounted
    opts: defaults,noatime

- name: Create data subdirectory
  ansible.builtin.file:
    path: "{{ actual_budget_data_dir }}/data"
    state: directory
    mode: "0750"

- name: Template docker-compose.yml
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: "{{ actual_budget_data_dir }}/docker-compose.yml"
    mode: "0640"
  notify: Restart actual_budget

- name: Pull Actual Budget image
  community.docker.docker_compose_v2_pull:
    project_src: "{{ actual_budget_data_dir }}"

- name: Start Actual Budget stack
  community.docker.docker_compose_v2:
    project_src: "{{ actual_budget_data_dir }}"
    state: present
