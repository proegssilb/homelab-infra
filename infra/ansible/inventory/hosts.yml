---
# Static inventory for physical hosts that are not Proxmox VMs/containers.
# The Proxmox dynamic inventory (proxmox.yml) covers everything else.

all:
  children:
    bootstrap:
      hosts:
        blade02:
