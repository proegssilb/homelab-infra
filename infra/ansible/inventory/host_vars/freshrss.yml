---
# Generate: openssl rand -hex 32 | ansible-vault encrypt_string --name freshrss_db_pass
freshrss_db_pass: "changeme-use-vault"

# Generate: openssl rand -hex 32 | ansible-vault encrypt_string --name freshrss_oidc_client_secret
# Must match TF_VAR_oidc_client_secrets["freshrss"] in .env
freshrss_oidc_client_secret: "changeme-use-vault"
